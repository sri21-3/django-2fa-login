-- Initialization handled by POSTGRES_DB in docker-compose.yml
-- You can place additional SQL here that should run once on first init,
-- e.g., creating extensions:
--   CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
-- or setting roles/permissions as needed.

